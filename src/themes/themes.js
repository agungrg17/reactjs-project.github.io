import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'dark',
        primary: {
          main: '#e593dd',
        },
        secondary: {
          main: '#93e59c',
        },
      },
});

export default theme;